
const Clientslayout = ({children}: {children: React.ReactNode}) => {
  return (
    <div>
        <h2>Layout de clientes</h2>
        {children}
    </div>
  )
}

export default Clientslayout
